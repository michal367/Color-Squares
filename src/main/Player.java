package main;

import javafx.scene.paint.Color;


public class Player extends Character {
    public Player(Vector2d startPosition, Color color, GameMap map) {
        super(startPosition, color, map);
    }
}
