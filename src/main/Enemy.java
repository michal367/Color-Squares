package main;

import javafx.scene.paint.Color;

public class Enemy extends Character {
    public Enemy(Vector2d startPosition, Color color, GameMap map) {
        super(startPosition, color, map);
    }
}
